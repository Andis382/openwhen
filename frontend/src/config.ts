/** App-wide constants. The demo accounts are created by the backend's seeder in demo mode. */
export const DEMO = {
  email: 'demo@openwhen.test',
  driverEmail: 'driver1@openwhen.test',
  password: 'demo1234',
}

/** Roles an owner can hand out with a join link. */
export const INVITE_ROLES: string[] = ['DRIVER', 'DISPATCHER']
